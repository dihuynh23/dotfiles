# Change prefix from Ctrl-b to Ctrl-a
set -g prefix C-a
unbind C-b
bind C-a send-prefix

# Prompt for window name on creation
set-window-option -g automatic-rename off
set-option -g allow-rename off
bind-key C command-prompt -p "Window name:" "new-window; rename-window '%%'"

# Prompt for changing pane title 
bind-key T command-prompt -p "Pane title:" "select-pane -T '%%'"

# Enable mouse support
set -g mouse on

# Start windows/panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1

# Faster escape time
set -sg escape-time 0

# Increase history limit
set -g history-limit 10000

# Copy to system clipboard on mouse selection
bind -T copy-mode MouseDragEnd1Pane send -X copy-pipe-and-cancel "xclip -selection clipboard"

# Enable notification
set -g visual-bell off
set -g bell-action any

# Allow passthrough for kitty notifications
set -g allow-passthrough on

# Dim inactive panes
set -g window-style 'fg=#878787,bg=#333333'
set -g window-active-style 'fg=#FFFFFF,bg=#1c1c1c'

# Pane border styling - rose pink from kitty 1989 theme
set -g pane-border-style fg=#444444
set -g pane-active-border-style fg=#f591b2

# Pane status bar - shows pane index, command, title (for claude), and path
set -g pane-border-status top
set -g pane-border-format ' #[fg=#f591b2]#{pane_index}#[default] #{pane_current_command}#{?#{==:#{pane_current_command},claude}, · #{pane_title},} #[fg=#878787]#{b:pane_current_path}#[default] '
